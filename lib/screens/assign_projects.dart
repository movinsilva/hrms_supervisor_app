import 'package:flutter/material.dart';
import 'package:hrms_supervisor_app/widgets/widget_library.dart';

class AssignProjects extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return DefaultBackground(
      interior:   Expanded(
          child: TextFormField()
        ),

    );
  }
}
